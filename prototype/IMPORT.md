# Importar o protótipo HTML aprovado

No seu PC:

`C:\Users\agtic15\Downloads\Valhalla Catálogo Prototipo HTML`

1. Copie o conteúdo para `prototype/import/` neste repo **AdminPriston**.
2. Se for um único `.html`, salve como `prototype/import/Valhalla-Catalogo.dc.html`.
3. Assets em `prototype/import/assets/`.

O layout Blade está em `resources/views/layouts/admin.blade.php`. Use o HTML como referência visual; as rotas Laravel em `/painel` são a implementação.
